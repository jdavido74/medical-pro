╔════════════════════════════════════════════════════════════════════════════╗
║                                                                            ║
║           🏥 SCRIPTS DE GESTION MEDICALPRO - RÉSUMÉ COMPLET               ║
║                                                                            ║
╚════════════════════════════════════════════════════════════════════════════╝

📍 LOCALISATION: /var/www/medical-pro/

📋 FICHIERS CRÉÉS:

1. ✅ start-all.sh
   ├─ Démarre tous les services en arrière-plan
   ├─ Simple et léger (pas de dépendances)
   └─ Idéal pour: débogage, développement simple

2. ✅ start-all-tmux.sh
   ├─ Démarre tous les services avec tmux
   ├─ 4 fenêtres séparées pour chaque service + monitoring
   └─ Idéal pour: production, monitoring en temps réel (RECOMMANDÉ)

3. ✅ stop-all.sh
   ├─ Arrête proprement tous les services
   ├─ Libère les ports et nettoie les processus
   └─ Prérequis: aucun

4. ✅ status-all.sh
   ├─ Affiche l'état de tous les services
   ├─ Vérifie les ports et les processus
   └─ Affiche les commandes utiles

5. ✅ help.sh
   ├─ Affiche cette aide
   ├─ Guide rapide d'utilisation
   └─ Prérequis: aucun

6. ✅ SCRIPTS_README.md
   ├─ Documentation complète (très détaillée)
   ├─ Dépannage et exemples avancés
   └─ À lire pour une compréhension complète

════════════════════════════════════════════════════════════════════════════

🚀 DÉMARRAGE RAPIDE:

   Option 1 - Simple (léger):
   ───────────────────────────
   $ cd /var/www/medical-pro
   $ ./start-all.sh

   Option 2 - tmux (RECOMMANDÉ):
   ────────────────────────────
   $ cd /var/www/medical-pro
   $ ./start-all-tmux.sh
   
   (Nécessite tmux: sudo apt install tmux)


🌐 SERVICES DÉMARRÉS:

   Frontend     : http://localhost:3000
   Backend API  : http://localhost:3001
   Admin Panel  : http://localhost:3002


📊 VÉRIFIER L'ÉTAT:

   $ ./status-all.sh


⏹️  ARRÊTER LES SERVICES:

   $ ./stop-all.sh


📚 AIDE:

   $ ./help.sh
   ou
   $ cat SCRIPTS_README.md


════════════════════════════════════════════════════════════════════════════

⚙️  STRUCTURE DES SERVICES:

/var/www/medical-pro/
├── package.json (React frontend)
├── npm start → port 3000

/var/www/medical-pro-backend/
├── package.json (Node.js/Express)
├── npm run dev → port 3001 (avec nodemon)
├── Dépend de PostgreSQL

/var/www/medical-pro-admin/
├── package.json (React admin panel)
├── npm start → port 3002


════════════════════════════════════════════════════════════════════════════

💡 CONSEILS D'UTILISATION:

• Toujours utiliser ./stop-all.sh avant de redémarrer
• Vérifier l'état avec ./status-all.sh régulièrement
• Si un port ne se libère pas, utiliser: pkill -f "npm"
• tmux est recommandé pour voir les logs en temps réel
• Les logs sont aussi disponibles dans /tmp/medicalpro-*.log


🔧 DÉPANNAGE COURANT:

Erreur: Port déjà utilisé
  → ./stop-all.sh && ./start-all.sh

Erreur: npm command not found
  → Installer Node.js: sudo apt install nodejs npm

Erreur: Module manquant
  → npm install
  → cd /var/www/medical-pro-backend && npm install
  → cd /var/www/medical-pro-admin && npm install

Erreur: tmux not found
  → Installer tmux: sudo apt install tmux
  → Ou utiliser ./start-all.sh (sans tmux)


════════════════════════════════════════════════════════════════════════════

📞 SUPPORT:

1. Vérifier les logs: ./status-all.sh
2. Consulter la documentation: cat SCRIPTS_README.md
3. Afficher l'aide: ./help.sh

════════════════════════════════════════════════════════════════════════════

✅ TOUS LES SCRIPTS SONT PRÊTS À ÊTRE UTILISÉS!

Pour commencer:
  $ cd /var/www/medical-pro
  $ ./start-all-tmux.sh
  OU
  $ ./help.sh

════════════════════════════════════════════════════════════════════════════
